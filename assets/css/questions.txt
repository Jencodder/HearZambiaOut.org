/* =========================================
   GLOBAL RESET + BODY
========================================= */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, sans-serif;
}

body {
    background: linear-gradient(to bottom, #004f8b, #0077c8);
    padding-bottom: 120px;
}

/* =========================================
   NAVIGATION BAR
========================================= */
.navbar {
    width: 100%;
    padding: 18px 40px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #002244;
    color: white;
    position: sticky;
    top: 0;
    z-index: 1000;
}

.logo-box {
    display: flex;
    align-items: center;
    gap: 12px;
}

.site-title {
    font-size: 1.5rem;
    font-weight: bold;
}

/* Desktop nav links */
.nav-links {
    display: flex;
    gap: 25px;
}

.nav-links a {
    text-decoration: none;
    color: white;
    font-size: 1rem;
    font-weight: 600;
    transition: color 0.3s ease;
}

.nav-links a:hover {
    color: #ffde59;
}

/* Burger menu for mobile */
.burger {
    display: none;
    cursor: pointer;
}

.burger-icon {
    width: 32px;
    height: 32px;
}

/* =========================================
   POSTS CONTAINER
========================================= */
.posts-container {
    width: 80%;
    max-width: 800px;
    margin: 80px auto;
    display: flex;
    flex-direction: column;
    gap: 25px;
}

/* =========================================
   POST CARD
========================================= */
.post-card {
    background: white;
    padding: 22px;
    border-radius: 12px;
    box-shadow: 0px 3px 8px rgba(0,0,0,0.15);
    border-left: 6px solid #004aad;
}

.post-content {
    font-size: 1.05rem;
    margin-bottom: 15px;
    color: #222;
    line-height: 1.6;
}

/* =========================================
   RESPONSE TEXTAREA & FORM
========================================= */
.response-form {
    margin-top: 10px;
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.response-form label {
    font-weight: bold;
    color: #004aad;
}

.response-form textarea {
    width: 100%;
    min-height: 120px;
    padding: 12px;
    font-size: 1rem;
    border-radius: 8px;
    border: 1px solid #bbb;
    resize: vertical;
}

/* Submit button */
.submit-btn {
    padding: 12px 16px;
    background: #004aad;
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: background 0.25s ease, transform 0.15s ease;
}

.submit-btn:hover {
    background: #003680;
    transform: translateY(-2px);
}

.submit-btn:active {
    transform: scale(0.97);
}

/* =========================================
   FOOTER
========================================= */
.footer {
    background-color: #002244;
    padding: 45px 20px;
    text-align: center;
    color: white;
    margin-top: 60px;
}

.footer-title {
    font-size: 1.3rem;
    font-weight: bold;
}

.social-links {
    display: flex;
    justify-content: center;
    gap: 25px;
    margin: 15px 0;
}

.social-icon img {
    width: 38px;
    height: 38px;
    transition: 0.3s ease;
}

.social-icon img:hover {
    transform: scale(1.2);
    filter: drop-shadow(0 0 10px white);
}

.footer-copy {
    opacity: 0.7;
    font-size: 0.9rem;
}

/* =========================================
   RESPONSIVE DESIGN
   (Tablet + Mobile)
========================================= */
@media (max-width: 900px) {
    .navbar {
        padding: 15px 25px;
    }

    .posts-container {
        width: 90%;
    }
}

@media (max-width: 768px) {
    /* Show burger */
    .burger {
        display: block;
    }

    /* Hide desktop nav */
    .nav-links {
        position: fixed;
        top: -100%;
        left: 50%;
        transform: translateX(-50%);
        width: 85%;
        background: #002244;
        flex-direction: column;
        align-items: center;
        padding: 25px 0;
        gap: 25px;
        border-radius: 12px;
        transition: top 0.4s ease;
        z-index: 2000;
    }

    .nav-links.open {
        top: 75px;
    }

    .posts-container {
        margin-top: 40px;
        width: 95%;
    }
}

@media (max-width: 480px) {
    .site-title {
        font-size: 1.2rem;
    }

    .post-card {
        padding: 18px;
    }

    .response-form textarea {
        min-height: 140px;
    }
}
